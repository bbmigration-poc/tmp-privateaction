version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2024.01
    steps:
      - checkout
      - run: echo "Dummy build - CircleCI activation smoke test"

workflows:
  build-workflow:
    jobs:
      - build
